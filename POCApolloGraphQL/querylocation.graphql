query LocationQuery($ip: String!) {
  getLocation(ip: $ip) {
    country {
      names {
        en
      }
      geoname_id
      iso_code
    }
    location {
      latitude
      longitude
    }
  }
}
